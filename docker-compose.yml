services:
  postgres:
    image: postgis/postgis:16-3.4
    ports: ["5432:5432"]
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: transport_dashboard
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
